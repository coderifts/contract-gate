{
  "name": "@coderifts/contract-gate",
  "version": "0.8.0",
  "private": true,
  "description": "GitHub Action that fails its check unless a valid signed ALLOW receipt exists for the PR's actual head diff, verified offline against a pinned keyring. Required in branch protection, that failing check blocks the merge.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/coderifts/contract-gate.git"
  },
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "release:check": "node scripts/assert-changelog-version.js"
  },
  "files": [
    "action.yml",
    "src",
    "keyring",
    "README.md",
    "LICENSE"
  ]
}
