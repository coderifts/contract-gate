# ONE TAG, PLUS ONE FILE THAT IS NOT IN IT.
#
# This block used to open "Mixed pin (1342). A single source-commit would lie", and then named
# verify.js as receipt-verifier 6048195 with bytes 363e52c7. BOTH HALVES HAD STOPPED BEING TRUE
# and the file contradicted its own hash lines: the pinned verify.js below is 348d3f85, and so are
# the bytes on disk. The v1.0.2 re-pin recorded further down superseded this paragraph; nobody
# deleted it, so the first thing a reader met was the state the file had moved away from.
#
# MEASURED 2026-09-13, every pinned entry against `git show v1.0.3:<path>` in receipt-verifier:
#   the ELEVEN .js files below — verify, arity, verify-bundle, verify-grant, verify-attest,
#   verify-toolset, verify-atomic-attestation, evidence-root, verify-evidence,
#   verified-execution-binding, verify-prove-transcript — are ALL byte-identical to the tag.
#   Not "mostly", not "the core": all of them. The set is no longer mixed, so the sentence that
#   a single source-commit would lie is itself now the lie.
#
# The one genuine exception, and it keeps its own note because it is still true:
#   xlang      = this tree's copy of the generator output (3c323e74). The file is
#                NOT in receipt-verifier (ephemeral-key generator). Hashing a
#                missing receipt-verifier/xlang-vectors.json is empty-string sha
#                e3b0c442 — that is not a pin of a real corpus.
#
# This is a comment correction only. VENDOR.sha256 is not itself under a byte pin — it cannot
# hash itself and no other repository pins it — so no hash line moves and no vendored byte moves.
# ── THE RECEIPT-VERIFIER CORE IS PINNED TO A RELEASED TAG ───────────────────────────────────
#
# The shared verification core here comes from receipt-verifier **v1.0.3**, peeled commit
# 523e0a2b822088990f500b9eea3868309439c53c.
#
# It was pinned to a WORKING TREE and to per-file commits. Both are honest about where the bytes
# came from and neither is a provenance a stranger can resolve: a working tree is not fetchable,
# so "the vendored bytes match upstream" reduced to "they match whatever is on this machine right
# now". A released tag is a name anyone can check out — that is the difference between a pin and
# a note.
#
# Hash lines that moved: verify.js, verify-grant.js (v1.0.3 admits applied_policy_hash).
# Every other hash line is unchanged because those files are byte-identical to v1.0.2.
#
#
# ── AND THE TAG IS SIGNED, WHICH IS THE PART A STRANGER CAN CHECK ───────────────────────────
#
# v1.0.0 was an unsigned tag: a name anyone can check out, and also a name anyone with push access
# can move. v1.0.1 is annotated and SSH-signed, so the pin now resolves to an identity and not
# only to bytes.
#
#   tag object   bb5753681275920e7e23351c6b41632957e6238f
#   peeled commit 523e0a2b822088990f500b9eea3868309439c53c
#   signer       zsobpeter@gmail.com
#   fingerprint  SHA256:7yRXTm9zKGicfFpzL+7lpwFoPaoSwxAJlabB3jwxw2Y
#
# Verify it yourself, in a checkout of receipt-verifier:
#
#   git tag -v v1.0.3
#   -> Good "git" signature for zsobpeter@gmail.com with ED25519 key SHA256:7yRXTm9zKGicfFpzL+7lpwFoPaoSwxAJlabB3jwxw2Y
#
# WHY v1.0.3 AND NOT v1.0.2: the CORE IS NOT byte-identical. verify-grant.js admits
# applied_policy_hash on V2_RESERVED_INERT (1942 verifier-admits; fail-closed otherwise
# unchanged) and verify.js moved with it. This re-pin copies those bytes from the signed
# tag, not from a working tree.
#
# The peeled commit is 523e0a2 — the K-phase-1 inert-admit commit the tag names.
# source_commit: 523e0a2b822088990f500b9eea3868309439c53c (receipt-verifier v1.0.3, signed)
#

verify.js  52d7c37611f9eecc862e3678d1ffcb4fd860ea4c5faf9a9df6d6d5b290bd8197
arity.js   721fcac58b960b5e2aa1af91edb6cfc87ebe828a6da73879967ef79a1ca474e8
test/fixtures/xlang-vectors.json  3c323e7469113093020498c8948185006783857e71ca01155d7cb10baedaf1a7

# bundle grading, vendored from receipt-verifier d69ab538785e3ee2185d02153e883e2d147e041e
verify-bundle.js  190de0dda70c4d15dcd21fb6bb6b279313e48f90bf2e477463580968d0987127
verify-grant.js  ea55b8611d6f3bba2cb9c84d765ade15bb2afe2418499e2da6358ffb3faf6ad6
verify-attest.js  035105de0f163d112038624eb2bf93cee059a565216fd8b03489964f6b0b2fa8
verify-toolset.js  2f0987a28f3e7d3758d71f59799d1a5c6190aaa882f8f887809b2bff434e2df8
verify-atomic-attestation.js  8ef57d4c3d7882f8f9215461cee7d582520ced18d467ba083e05b1aa9c54b1f7
evidence-root.js             4d5476fc44bbfc2a847feeb313a1c6c08ef7fd832a2348ef3c244bd8d3f03fcd
verify-evidence.js           98cd9b989a25095e0442f91f68a1adecd6e6a2a5b05902bf2580b8cba973c5e2
verified-execution-binding.js 9d0259e916f2bcd32029744c3e9ec81448bcefa46e6ad3f054c8a650dc0482b7
verify-prove-transcript.js   3bb17bd4f16d5ee3cf3fbb754b4df9aa570a615ca94ff2a97b751c4fabd3bd0d

